[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes so client-side router works (no 404 on refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
